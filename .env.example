# Cloudflare Tunnel token (required only for deployment with --profile tunnel)
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# Optional: pin a specific image tag instead of :latest
# IMAGE_TAG=0.1.0
